version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - aws s3 cp s3://aip-amplify/.env.stg .env.stg
            - npm ci --cache .npm --prefer-offline
        build:
          commands:
            - NODE_ENV=stg npm run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - .next/cache/**/*
          - .npm/**/*
    appRoot: aip-frontend
